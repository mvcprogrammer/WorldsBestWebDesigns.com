<body>
    @{Html.RenderAction("navbar", "Common");}
    @{Html.RenderAction("breadcrumb");}
    @{Html.RenderAction("portfolio");}
    @{Html.RenderAction("callToAction");}
    @{Html.RenderAction("footer", "Common");}
    @{Html.RenderAction("scripts", "Common");}
</body>